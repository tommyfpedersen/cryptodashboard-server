<div class="card" id="stats-pbaas-block-height-card">
    <img width="16px" src="/img/stats.png" />
    <button class="hide-button">x</button>
    <div class="card-content">
        <div class="card-title">PBaaS Block Height</div>
        {{!-- {{#if vrscOnline}} --}}
        {{#each blockheightArray}}
        <div class="card-row-wrapper">
            <div class="card-row-item">{{{this.blockchain}}}</div>
            <div class="card-row-item">
                 {{{toFixed this.blockheight 0}}}
            </div>
        </div>
        {{/each}}

    </div>
</div>